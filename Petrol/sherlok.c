#include <stdio.h>
int main()
{
int n;
 int arr[100];
scanf("%d" , &n);
    for (int i = 0; i < n; i++)
    {

        if ((arr[i] >= 'A' && arr[i] <= 'Z') && (arr[i + 1] >= 'a' && arr[i + 1] <= 'z'))
        {
            int count = 0;
            count++;
        }
    }
    return 0;
}